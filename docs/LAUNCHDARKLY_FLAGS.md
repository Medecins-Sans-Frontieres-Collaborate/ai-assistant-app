# LaunchDarkly feature flags

Every flag the app reads, what it gates, its fail-behavior when LaunchDarkly
is unreachable or the flag is unserved, and what to serve per environment.

## How flags are wired

- **Client-side only.** All flags are read in React components via `useFlags()`
  from `launchdarkly-react-client-sdk`. There are no server-side flag reads.
- **Provider**: `components/Providers/AppProviders.tsx` mounts `LDProvider`
  with `bootstrap: 'localStorage'`. When `NEXT_PUBLIC_LAUNCHDARKLY_CLIENT_ID`
  is not configured (e.g. local dev), the provider is skipped entirely and
  every flag evaluates to `undefined` — which is why the fail-open/fail-closed
  conventions below matter.
- **Key casing**: the React SDK camel-cases flag keys by default
  (`useCamelCaseFlagKeys`), so a dashboard key `explore-bots` arrives in code
  as `exploreBots`. The names below are the CODE names; check the LD project
  for whether the dashboard key is the kebab-case or camelCase form before
  creating a new one.
- **Targeting context** (`kind: 'user'`): `key` (user id), `email`,
  `givenName`, `surName`, `displayName`, `jobTitle`, `department`,
  `companyName`. Admin-ish gating is done with LD targeting rules on these
  attributes (e.g. department/companyName), not app-side role checks.
- **Env vars**: `NEXT_PUBLIC_LAUNCHDARKLY_CLIENT_ID` (client SDK id, reaches
  the browser) and `LAUNCHDARKLY_SDK_KEY` (server SDK key — currently unused
  by app code). Both optional in `config/environment.ts`.

## Conventions

- **Fail-open** (`flag !== false`): the feature is ON when the flag is
  `undefined` (LD unconfigured/unserved). Used for established features so an
  LD outage doesn't turn the product off. To disable, serve `false` explicitly.
- **Fail-closed** (`flag === true`): the feature is OFF unless the flag is
  explicitly `true`. Reserved for risky surfaces where an outage must degrade
  to "off". Currently `mcpArbitraryServers`, `conversationWorkflows`,
  `enableBYOModels`, `enableEncryptedBackups`, `enableMemories`, and
  `localModels`.

## Flag reference

| Code name                  | Gates                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Convention                   | Read in                                                                                                                                                                                                                          | Recommended serving                                                                                                                                                                                                                                                                                  |
| -------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `exploreBots`              | Org-managed agent discovery in the model picker's Agents tab: regional/office Foundry projects + static agents from `organization-agents.json`. User-connected (BYO) sources are deliberately NOT gated by this.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Fail-open                    | `components/Chat/ModelSelect.tsx`, `components/Chat/ModelSelect/AgentsTab.tsx`                                                                                                                                                   | `true` everywhere (established feature). Serve `false` only to hide org agents in an environment.                                                                                                                                                                                                    |
| `enableClaudeModels`       | Visibility of `claude-*` models in the model picker (provider `anthropic` filtered out when off). Does not affect existing conversations server-side.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Fail-open                    | `components/Chat/ModelSelect.tsx`                                                                                                                                                                                                | `true` where the Anthropic Foundry deployments exist; `false` where they don't (e.g. environments still missing claude-\* deployments).                                                                                                                                                              |
| `agentSourceBrowse`        | The "browse Azure resources" discovery mode in the BYO Foundry connection form (`AgentSourceForm`). When off, users must enter resource paths manually.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Fail-open                    | `components/Chat/AgentSources/AgentSourceForm.tsx`                                                                                                                                                                               | Serve `false` in prod until comms go-ahead; `true` in beta.                                                                                                                                                                                                                                          |
| `showUsageImpact`          | The "Usage & Impact" settings section (local token/CO2e stats) in the settings sidebar.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Fail-open                    | `components/Settings/SettingsSidebar.tsx`                                                                                                                                                                                        | `true` once the section is announced; `false` to hide.                                                                                                                                                                                                                                               |
| `mcpConnectors`            | The entire "Connectors" settings section (curated MCP catalog + arbitrary servers area): sidebar item, mobile nav entries. UI-gating only — servers a user already connected keep being sent with chat requests if the flag is later turned off (flag-off means "stop acquisition", not "break existing users").                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Fail-open                    | `components/Settings/SettingsSidebar.tsx`, `components/Settings/MobileNavigation.tsx`, `components/Settings/MobileSettingsHeader.tsx`                                                                                            | Serve `false` in prod until the MCP rollout go-ahead; `true` in beta/dev.                                                                                                                                                                                                                            |
| `mcpArbitraryServers`      | Arbitrary (non-catalog) MCP servers: BOTH the settings UI area (user opt-in toggle + add/edit form) AND the chat send path — `AppInitializer` mirrors the flag into `settingsStore.mcpArbitraryFlagEnabled`, and `chatStore` refuses to send arbitrary servers unless the mirror is `true`. Flipping to `false` stops arbitrary servers being sent immediately, without a reload.                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Fail-closed** (`=== true`) | `components/Settings/Sections/ConnectorsSection.tsx`, `components/Providers/AppInitializer.tsx`                                                                                                                                  | Serve `true` only where arbitrary MCP servers are explicitly sanctioned. Note the server has an additional independent gate: env `MCP_CUSTOM_SERVERS_ENABLED` (default `false`) must also be set for `/api/mcp/*` to accept custom URLs.                                                             |
| `structuredDataExtraction` | The structured-data-extraction UI: the "Extract" toggle in the chat-input Dropdown, the ExtractionTray (recipe-chip row above the composer), and the "Recipes" tab in the Quick Actions (Customizations) modal. UI-gating only — with every entry point hidden the client produces no `extraction` payload, so the server enricher (`ExtractionEnricher.shouldRun()` returns `!!context.extraction`) stays dormant; no server flag read is needed.                                                                                                                                                                                                                                                                                                                                                                                        | Fail-open                    | `components/Chat/ChatInput/Dropdown.tsx`, `components/Chat/ChatInput.tsx`, `components/QuickActions/CustomizationsModal.tsx`                                                                                                     | Serve `false` in prod until the extraction rollout go-ahead; `true` in beta/dev.                                                                                                                                                                                                                     |
| `conversationWorkflows`    | Workflow-conversation entry points: the per-conversation workflow mode switcher (`WorkflowTabs`) and the workflow items in the sidebar new-chat dropdown. Gates creation ONLY — existing workflow conversations always render their workflow window (the page-level branch reads `conversationType` off the conversation, never this flag), mirroring the `mcpConnectors` "stop acquisition, don't break existing users" rule. No server-side gating: `/api/workflows/*` routes are reachable regardless of the flag (client-side LD only; accepted v1 gap). With LD unconfigured locally the flag is `undefined`, so entry points are hidden — serve or bootstrap the flag to test.                                                                                                                                                      | **Fail-closed** (`=== true`) | `components/Workflows/WorkflowTabs.tsx` (`useWorkflowTabsEnabled`), `components/Sidebar/Sidebar.tsx`                                                                                                                             | Serve `false` in prod until rollout; `true` in beta/dev. Note `useWorkflowTabsEnabled` additionally returns `true` on `localhost`/`127.0.0.1` so the tabs are testable with LD unconfigured.                                                                                                         |
| `enableBYOModels`          | Allows users to connect to their own Azure Foundry instances to use models they'd prefer or their own setup. Gates the "Connect a model source" button ONLY — already-connected sources keep rendering when the flag is off, per the `mcpConnectors` "stop acquisition, don't break existing users" rule.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Fail-closed** (`=== true`) | `components/Chat/ModelSelect.tsx`                                                                                                                                                                                                | Serve `false` in prod until rollout; `true` in beta/dev.                                                                                                                                                                                                                                             |
| `enableEncryptedBackups`   | E2E-encrypted chat backup, client-side only: the enrollment auto-prompt + all backup modals (`BackupModals` renders null unless `true`, and mirrors the flag into `backupStore.flagEnabled` so the sync engine stays dormant), the key-mismatch/remote-missing banner, and the Backup settings section in the sidebar. The `/api/backup/*` routes are INTENTIONALLY auth-only with no flag/env gate (LD is client-only in this app; the routes only touch the caller's own `${userId}/backup/` blob prefix) — flag-off means no client ever calls them, not that they 404.                                                                                                                                                                                                                                                                | **Fail-closed** (`=== true`) | `components/Backup/BackupModals.tsx`, `components/Settings/SettingsSidebar.tsx`                                                                                                                                                  | Serve `false` in prod until the backup rollout go-ahead; `true` in beta/dev.                                                                                                                                                                                                                         |
| `enableMemories`           | Cross-conversation Memories, client-side only: the Memories settings section in the sidebar, post-exchange memory-extraction calls, and the send-path `memories` field — `AppInitializer` mirrors the flag into `settingsStore.memoriesFlagEnabled`, and both extraction and injection additionally require the persisted user opt-in `memoriesEnabled` (default off). Extraction alone additionally requires `memoryCapturePaused` to be false — that setting pauses learning without stopping saved memories from being injected, so it deliberately does NOT gate the send path. Flipping to `false` stops extraction/injection immediately, without a reload. The `/api/chat/memories` route is INTENTIONALLY auth-only with no flag gate (LD is client-only in this app) — flag-off means no client ever calls it, not that it 404s. | **Fail-closed** (`=== true`) | `components/Settings/SettingsSidebar.tsx`, `components/Providers/AppInitializer.tsx`                                                                                                                                             | Serve `false` in prod until the memories rollout go-ahead; `true` in beta/dev.                                                                                                                                                                                                                       |
| `localModels`              | Local model runtimes (Ollama, LM Studio, llama.cpp) end to end: runtime detection, the "Local models" settings section, the per-runtime sections in the model picker, and the send path — `AppInitializer` mirrors the flag into `settingsStore.localModelsFlagEnabled`, which `useLocalRuntimeModels` requires before it will probe or expose any model. Unlike `enableBYOModels` this is NOT acquisition-only: local models exist only while their runtime is detected (status is session-scoped, never persisted), so flag-off simply means no local models appear — there is no population of existing users to break. This is the feature's ONLY kill switch, which is why it is fail-closed: the transport is browser-direct to loopback and depends on browser behavior we cannot control or detect (see the LNA note below).      | **Fail-closed** (`=== true`) | `components/Settings/SettingsSidebar.tsx`, `components/Providers/AppInitializer.tsx`, `components/Chat/ModelSelect.tsx` (via `client/hooks/useLocalRuntimeModels.ts`)                                                            | Serve `false` in prod until the Local Network Access spike passes on corp-managed Chrome AND Edge (see below); `true` in beta/dev.                                                                                                                                                                   |
| `m365Files`                | Microsoft 365 file features, client-side only: the "Attach from OneDrive / SharePoint" chat-input entry + picker modal, the "Save to OneDrive" section in export menus, and (with `m365Mail`) the Settings → Connections section. Every entry point additionally requires the per-user connect opt-in (`settingsStore.m365Connected`, default off). The `/api/m365/*` routes are INTENTIONALLY auth-only with no flag gate (LD is client-only in this app; every route acts through the caller's own delegated Graph token) — flag-off means no client ever calls them. Tenant admin consent is a third, independent gate: without it the routes return `M365_CONSENT_MISSING` and the UI shows "pending admin approval".                                                                                                                 | **Fail-closed** (`=== true`) | `client/hooks/useM365Enabled.ts` (consumed by `components/Chat/ChatInput/Dropdown.tsx`, `components/Settings/useSettingsNav.ts`, `client/hooks/document/useM365Save.tsx`, `components/Settings/Sections/ConnectionsSection.tsx`) | Serve `false` in prod until the M365 consent tickets land + comms go-ahead; `true` in beta/dev once dev consent is granted. Note `useM365Enabled` additionally returns `true` on `localhost`/`127.0.0.1` so the features are testable with LD unconfigured (same hatch as `useWorkflowTabsEnabled`). |
| `m365Mail`                 | Microsoft 365 email import, client-side only: the "Import email…" chat-input entry + mailbox search modal, and (with `m365Files`) the Settings → Connections section. Read-only (`Mail.Read`): messages/threads import as markdown attachments; attachment contents are never fetched. Same three-gate model as `m365Files` (flag + per-user `m365Connected` opt-in + tenant consent), and the same intentionally auth-only route posture.                                                                                                                                                                                                                                                                                                                                                                                                | **Fail-closed** (`=== true`) | `client/hooks/useM365Enabled.ts` (consumed by `components/Chat/ChatInput/Dropdown.tsx`, `components/Settings/useSettingsNav.ts`, `components/Settings/Sections/ConnectionsSection.tsx`)                                          | Serve `false` in prod until the M365 consent tickets land + comms go-ahead; `true` in beta/dev once dev consent is granted. Note `useM365Enabled` additionally returns `true` on `localhost`/`127.0.0.1` so the features are testable with LD unconfigured (same hatch as `useWorkflowTabsEnabled`). |
| `m365Agents`               | M365 file-backed shareable agents (docs/M365_SECOND_PASS_AGENTS_DESIGN.md), client-side only: the admin "Microsoft 365 agents" block in the agents section, the chat-side source-access banner, and (indirectly) whether admins can create them. Server enforcement is INDEPENDENT of this flag: /api/agent-access/m365-agents is gated by `AGENT_ACCESS_CONTROL_ENABLED`, and chat invocations run the two-layer access guard regardless — flag-off means no UI creates or surfaces the agents, not that the guard is off. Discovery entries (type 'm365') still serve; ModelSelect renders them, so flip the flag only alongside an empty agent store or accept read-only exposure.                                                                                                                                                     | **Fail-closed** (`=== true`) | `client/hooks/useM365Enabled.ts` (consumed by `components/AgentAccess/M365AgentsSection.tsx`, `components/Chat/M365AgentAccessBanner.tsx`)                                                                                       | Serve `false` in prod until rollout; `true` in beta/dev. Same localhost hatch as the other m365 flags.                                                                                                                                                                                               |

## Related non-LD gates (for completeness)

These are environment variables, not LD flags, but they interact with the
flags above:

- `MCP_CUSTOM_SERVERS_ENABLED` — server-side defense-in-depth for arbitrary
  MCP server URLs (`/api/mcp/tools`, `/api/mcp/oauth/*`, chat tool loop).
  Both this AND `mcpArbitraryServers` must be on for arbitrary servers to work.
- `NEXTAUTH_URL` — required for MCP OAuth connectors (it is the configured
  origin used to build OAuth redirect URIs; never derived from request Host).
  It must be the origin users actually browse.
- `MCP_OAUTH_GITHUB_CLIENT_ID/_SECRET`, `MCP_OAUTH_ASANA_CLIENT_ID/_SECRET` —
  deployment-wide pre-registered OAuth apps for the curated connectors.
  Needed for OAuth on deployed origins: GitHub has no dynamic client
  registration, and Asana's DCR only accepts loopback redirect URIs
  (localhost dev works without these). Register each app with redirect URI
  `${NEXTAUTH_URL}/mcp-oauth-callback`. These are OPTIONAL even in prod:
  users can alternatively supply their OWN OAuth app per connector ("Use
  your own OAuth app" in Connectors — needed anyway for users on different
  provider instances/orgs), whose credentials live in their localStorage
  like PATs.
- `NEXT_PUBLIC_MODEL_DISCOVERY_ENABLED` — live Foundry model discovery
  (see `docs/MODEL_DISCOVERY_DESIGN.md`).

### Browser-level gates for `localModels`

Local model runtimes are reached **browser-direct** over `http://127.0.0.1:<port>`
— the app is deployed, so a server-side fetch to localhost would hit the
container, not the user's machine. Three gates sit outside the LD flag, and
none of them is controllable from app code at runtime:

- **CSP** (`next.config.js`) — `connect-src` includes `http://127.0.0.1:*` and
  `http://localhost:*`. This widening is unconditional: a static header cannot
  be flag-gated. Low risk (loopback reachability adds little for an attacker
  who can already run script in the page), but call it out at review.
- **Chrome Local Network Access (LNA)** — Chrome 142+ (Oct 2025) gates
  public→loopback requests behind a user permission prompt, and managed
  Chrome/Edge can block or pre-allow it by policy
  (`LocalNetworkAccessBlockedForUrls` / `LoopbackNetworkAccessAllowedForUrls`).
  Firefox and Safari do not implement LNA and treat `http://localhost` as
  potentially trustworthy, so they work with no prompt.

  **If enterprise policy blocks loopback there is no application-side
  workaround** — not a proxy, not a header. The remedy is asking IT to add the
  app origin to `LoopbackNetworkAccessAllowedForUrls`. Verify before serving
  the flag `true` anywhere users are on managed browsers: with Ollama running
  and `OLLAMA_ORIGINS` set to the app origin, run
  `await fetch('http://127.0.0.1:11434/v1/models')` from a corp-managed Chrome
  AND Edge. Prompt → grant → 200 means good; silently blocked means policy.

  Because a denied prompt is sticky and awkward to reverse, detection is
  deliberately never automatic — the user triggers it from Settings › Local
  models, so the prompt arrives in context.

- **CORS on the runtime** — each runtime must allow the app origin
  (`OLLAMA_ORIGINS` for Ollama, a CORS toggle for LM Studio; llama.cpp varies
  by build, hence its "experimental" label). The probe issues two requests
  specifically so "not running" and "running but CORS-blocked" can be told
  apart and coached — from one failed fetch they are the same opaque
  `TypeError`.

## Adding a new flag

1. Read it with `const { myFlag } = useFlags()` and pick the convention
   deliberately: `!== false` (fail-open) for product features, `=== true`
   (fail-closed) for risky/exfiltration-adjacent surfaces — add a one-line
   comment justifying fail-closed at the call site.
2. If a vanilla (non-React) store needs the value, mirror it into a
   runtime-only store field from `AppInitializer` (see
   `mcpArbitraryFlagEnabled` — NOT persisted, re-derived each session).
3. Document it in this file, and add the flag to the LD project for every
   environment before merging (an unserved flag evaluates as `undefined`).
4. Keep the "Read in" column honest — it is the only index of where a flag is
   consumed, and it silently rots when components are renamed or deleted.
   Verify with `grep -rn '<flagName>' components/ client/ app/` before
   merging a change that moves a flag read.
